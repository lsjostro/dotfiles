[Unit]
Description=Run wezterm mux server
After=gnome-session-wayland@gnome.target

[Service]
Restart=always
StartLimitIntervalSec=0
ExecStart=/usr/bin/wezterm-mux-server

[Install]
WantedBy=graphical-session.target
